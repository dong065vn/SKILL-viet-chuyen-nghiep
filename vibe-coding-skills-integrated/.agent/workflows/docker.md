---
description: Docker containerization
---

# /docker - Docker Setup

## Cách dùng
```
/docker [service type]
```

## Quy trình
1. Tạo Dockerfile (multi-stage)
2. Tạo docker-compose.yml
3. Cấu hình networks/volumes
4. Health checks

## Skill sử dụng
- `docker-expert` - Docker patterns

## Output
- `Dockerfile`
- `docker-compose.yml`
- `.dockerignore`
